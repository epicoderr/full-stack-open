# Zustand Anecdotes

This folder is based on the course starter `zustand-anecdotes` and implements exercises 6.2-6.15.

Run the JSON Server and frontend in separate terminals:

```bash
npm install
npm run server
npm run dev
```

Tests:

```bash
npm test
```
