# Query Anecdotes

This folder is based on the course starter `query-anecdotes` and implements exercises 6.16-6.22.

Run the JSON Server and frontend in separate terminals:

```bash
npm install
npm run server
npm run dev
```

The app uses TanStack Query for server state and Context API for notifications.
